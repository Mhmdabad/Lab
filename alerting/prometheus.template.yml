global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "nodejs-app"
    scheme: https
    static_configs:
      - targets: ["${API_HOST}"]

  - job_name: "node-exporter"
    static_configs:
      - targets: ["${API_HOST}:9100"]
